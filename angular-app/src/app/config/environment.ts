/**
 * Application Environment Configuration
 * Update the BACKEND_URL here to change the backend endpoint across the entire application
 */
export const environment = {
  /**
   * Backend API base URL
   * Change this URL to point to your backend server
   *
   * Examples:
   * - Production: 'https://web-sampler-angular-mdb.onrender.com'
   * - Local development: 'http://localhost:5000'
   */
  BACKEND_URL: 'https://web-sampler-angular-mdb.onrender.com',
} as const;
